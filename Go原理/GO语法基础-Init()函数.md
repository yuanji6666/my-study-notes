# 用处

init函数是一个特殊函数，在程序执行之前进行初始化工作，无需显式调用

# 执行顺序

- 同一包的init按照出现顺序执行  
```
import -> const -> var -> init() -> main()
```

- 不同包的init按照依赖关系执行，被依赖的先执行
- 同一个包多次导入，init只执行一次
# 应用场景

配置加载，数据库初始化，全局变量，单例模式初始化。。。